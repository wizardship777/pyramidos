#include <bits/stdc++.h>

using namespace std;

int main() {
    /*
        APPEND: TODO/FIX by \inf+ AD:
        //int -> bigint?
    */
    mt19937 rnd(time(0));//APPEND
    //WHO APPEND
    int n = rnd();//APPEND
    vector <double> mirage_of_life(n + 3);
    vector <double> mirage_of_game(n + 7);
    genetic_algorithms(mirage_of_life, mirage_of_game, DO YOU KNOW THE WAE KNUCKCLES CODE GENERATOR FUNCTIONS);
    //AGI ASI AGI ASI WHICH? HOW MANY? ORDER? APPEND ABOVE
    //WHO ABOVE?
    //APPEND above?
}
